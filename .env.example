# =============================================================================
# Auction Alerts - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to version control!
# =============================================================================

# =============================================================================
# SUPABASE
# =============================================================================
# Get these from: https://app.supabase.com → Your Project → Settings → API

# Your Supabase project URL
SUPABASE_URL=https://zmbawuqscckekdoivjdt.supabase.co

# Your Supabase service role key (NOT the anon key!)
# Service role key bypasses RLS - use only on server side
SUPABASE_KEY=sb_publishable_PAZkdTNaAgfbDD14Ig9Pqg_0f72vWrk

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================
# Choose one provider: "smtp" or "sendgrid"
EMAIL_PROVIDER=smtp

# SMTP Settings (for Gmail, Outlook, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
# For Gmail, use an App Password: https://myaccount.google.com/apppasswords
SMTP_PASSWORD=your-app-password

# SendGrid Settings (alternative to SMTP)
# SENDGRID_API_KEY=SG.your-sendgrid-api-key

# From address for alert emails
FROM_EMAIL=alerts@yourdomain.com
FROM_NAME=Auction Alerts

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Reference location (Miami by default)
MIAMI_LAT=25.7617
MIAMI_LNG=-80.1918

# Default intent parameters
DEFAULT_MAX_PRICE=1200.0
DEFAULT_MAX_DISTANCE=100.0
DEFAULT_CLOSING_HOURS=48
DEFAULT_CONFIDENCE_THRESHOLD=0.6

# Click tracking server URL (optional)
# If you run the tracking server, set this to enable click tracking
# TRACKING_BASE_URL=https://your-domain.com

# =============================================================================
# OPTIONAL: LOGGING
# =============================================================================
# LOG_LEVEL=INFO
